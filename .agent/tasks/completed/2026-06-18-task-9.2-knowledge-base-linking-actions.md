# Task: Task 9.2 - Implement knowledge base linking actions

Status: Completed

## Goal

Add Admin-side linking actions for knowledge base entries to posts and topics using the documented service API endpoints.

## Background

Knowledge base CRUD was already implemented, but the editor only displayed linked posts and topics as read-only counts. The updated contract now documents explicit add-link endpoints for posts and topics.

## Required Context

- `AGENTS.md`
- `.agent/INDEX.md`
- `.agent/MEMORY.md`
- `.agent/tasks/current-task.md`
- `.agent/agents/SHARED-INSTRUCTIONS.md`
- `.agent/TASK-WORKFLOW.md`
- `.agent/API-CONTRACT.md`
- `.agent/skills/api-client-integration.md`
- `.agent/skills/forms-validation.md`
- `.agent/skills/laravel-livewire.md`
- `openapi.json`

## Files To Inspect

- `openapi.json`
- `app/Services/WideWebBlogApi/Clients/KnowledgeBaseClient.php`
- `app/Livewire/Admin/KnowledgeBase/Editor.php`
- `resources/views/livewire/admin/knowledge-base/editor.blade.php`
- `tests/Feature/KnowledgeBase/KnowledgeBaseEditorTest.php`
- `tests/Unit/WideWebBlogApi/Clients/KnowledgeBaseClientTest.php`

## Files To Change

- `app/Services/WideWebBlogApi/Clients/KnowledgeBaseClient.php`
- `app/Livewire/Admin/KnowledgeBase/Editor.php`
- `resources/views/livewire/admin/knowledge-base/editor.blade.php`
- `tests/Feature/KnowledgeBase/KnowledgeBaseEditorTest.php`
- `tests/Unit/WideWebBlogApi/Clients/KnowledgeBaseClientTest.php`

## Implementation Steps

1. Add `linkPost` and `linkTopic` client methods for the documented `/admin/knowledge-base/{id}/link-post` and `/admin/knowledge-base/{id}/link-topic` endpoints.
2. Add focused linking form state and submit handlers to the knowledge base editor.
3. Refresh linked post/topic state from the returned resource after a successful link.
4. Map `422` errors into the correct linking fields without affecting the main editor save flow.
5. Add focused client and Livewire feature coverage for both linking actions.

## Acceptance Criteria

- [x] Knowledge base entries can be linked to posts from the editor using the documented endpoint
- [x] Knowledge base entries can be linked to topics from the editor using the documented endpoint
- [x] Linking validation errors map to the correct linking inputs
- [x] Existing entry save behavior remains unchanged

## Validation Commands

- `php artisan test tests/Unit/WideWebBlogApi/Clients/KnowledgeBaseClientTest.php`
- `php artisan test tests/Feature/KnowledgeBase/KnowledgeBaseEditorTest.php`

## Validation Results

- `php artisan test tests/Unit/WideWebBlogApi/Clients/KnowledgeBaseClientTest.php`
- `php artisan test tests/Feature/KnowledgeBase/KnowledgeBaseEditorTest.php`

## Risks

- The current contract only documents add-link actions, not unlink actions, so the UI stays explicit about that limitation.

## Completion Notes

- Added `linkPost` and `linkTopic` methods to `KnowledgeBaseClient`.
- Added dedicated `linkPostId` and `linkTopicId` editor state with isolated validation and error mapping.
- The knowledge base editor now supports linking a post or topic by ID from the sidebar.
- Linked items now render returned IDs and titles instead of only summary counts when the service provides them.
- Added focused unit and feature coverage for successful link actions and `422` validation mapping.
